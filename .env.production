# Auto-loaded by `npm run build`. Set this to your Railway API domain
# (Railway → API service → Settings → Networking → Generate Domain),
# then: npm run build && firebase deploy --only hosting
VITE_API_URL=https://your-api.up.railway.app
